import React from 'react'

//implement the  main layout for each different features if required only if not use the single global layout wrapper for whole 

const AdminLayout = () => {
  return (
    <div>AdminLayout</div>
  )
}
export default AdminLayout